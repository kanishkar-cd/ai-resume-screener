# Base requirements placeholder for Python 3.12 backend
# Dependencies (FastAPI, SQLAlchemy 2.0, Alembic, Pydantic v2, asyncpg, etc.) to be pinned during package setup.
fastapi
uvicorn[standard]

sqlalchemy>=2.0
alembic
asyncpg

pydantic>=2.0
pydantic-settings

python-dotenv

structlog

httpx

pytest
pytest-asyncio

python-multipart